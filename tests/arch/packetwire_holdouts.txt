# Client/Packet sources compiled into the DarkEden executable, not the
# packetwire library: the receive loops and connection state machines.
# Rule W0 in tests/arch/check_includes.pl requires every .cpp under
# Client/Packet to be in exactly one of this file and
# tests/arch/packetwire_files.txt.
#
# Neither the debug facilities nor the client's tuning values keep any
# of them out any more: task 5.1's first slice moved the logging header
# into basic/, and its second put the three tuning values they read
# behind WireHost. What each of the five still needs, as of 2026-09-03:
#
#   ClientPlayer.cpp                MZone.h, UserInformation.h
#   RequestClientPlayer.cpp         ClientDef.h, RequestFileManager.h
#   RequestClientPlayerManager.cpp  ClientDef.h, MPlayer.h,
#                                   MGameStringTable.h, ProfileManager.h,
#                                   RequestUserManager.h, ServerInfo.h,
#                                   UserInformation.h, WhisperManager.h
#   RequestServerPlayer.cpp         ClientDef.h, RequestFileManager.h
#   RequestServerPlayerManager.cpp  ClientDef.h, ServerInfo.h
#
# These are game state, not tuning: g_pGameMessage and g_pGameStringTable
# for the messages the request path shows the player, g_pPlayer,
# g_pUserInformation, g_pRequestFileManager, g_pProfileManager,
# g_pWhisperManager, g_pRequestUserManager - and ClientDef.h is wanted
# for g_Mode and the MODE_* enum, which four of them branch on. A host
# struct the size of WireHost would not cover that; the request-service
# family is its own slice.

Client/Packet/ClientPlayer.cpp
Client/Packet/RequestClientPlayer.cpp
Client/Packet/RequestClientPlayerManager.cpp
Client/Packet/RequestServerPlayer.cpp
Client/Packet/RequestServerPlayerManager.cpp
