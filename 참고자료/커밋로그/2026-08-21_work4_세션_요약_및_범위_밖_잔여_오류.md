# 커밋 로그 (세션 요약)

- 일시: 2026-08-20 ~ 2026-08-21
- 대상: `work 4 error.log` 기반 오류 수정 세션 전체 요약
- 유형: docs (요약, 코드 변경 없음)

## 결과

`참고자료/작업지시/work 4 error.log`(VS_UI + DarkEden 전체 재빌드 로그)를
바탕으로 서로 독립적인 근본 원인 10가지를 찾아 수정함. 상세 내용은 각각의
개별 커밋 로그 참고:

1. `2026-08-20_CMake_Ci_FL2_USE_SDL_BACKEND_제외_및_FL2_h_시그니처_수정.md`
2. `2026-08-20_MParty_h_vector_및_ValueList_h_typename_수정.md`
3. `2026-08-20_MTopView_h_CD3DTextureEffect_죽은_멤버_제거.md`
4. `2026-08-20_RenderingFunctions_cpp_2d_h_include_누락_수정.md`
5. `2026-08-20_Client_h_CWinUpdate_h_MMSystem_h_재정의_및_MM_MCINOTIFY_수정.md`
6. `2026-08-21_SetSurfaceInfo_GetDDSD_시그니처_불일치_일괄_수정.md`
7. `2026-08-21_WNDPROC_호출규약_캐스트_및_IDirectDraw_죽은_호출_제거.md`
8. `2026-08-21_UtilityFunction_LoadImageToSurface_CSpriteSurface_오버로드_활성화.md`
9. `2026-08-21_MHelpMessageManager_cpp_UTF8_BOM_추가.md`
10. `2026-08-21_MZone_cpp_builtin_expect_제거.md`

## 검증 (최종)

- `MSBuild build/vs2019/VS_UI.vcxproj /p:Configuration=Debug /p:Platform=x64`
- `MSBuild build/vs2019/DarkEden.vcxproj /p:Configuration=Debug /p:Platform=x64`
  (VS_UI를 프로젝트 참조로 먼저 빌드)
- 두 프로젝트 모두 아래 5건의 "범위 밖" 오류만 남기고 나머지는 전부 해소됨

## 참고 (범위 외 발견 사항 — 이번 작업에 포함하지 않음)

아래 5건은 실제 기능(윈도우/그래픽 초기화 로직)이 SDL 백엔드로 아직
포팅되지 않아 발생하는, 단순 오타/시그니처 문제가 아닌 **진짜 기능 구현
공백**임. 2026-08-20 `WinMain_class_ifstream_elaborated_type_specifier_수정.md`
및 `VS_UI_CDirectDraw_재정의_및_헤더_섀도잉_수정.md`에서 이미 같은 이유로
범위 밖으로 남겨두었던 항목과 동일한 성격이라, "재탐색 금지" 원칙에 따라
이번에도 손대지 않음:

- `VS_UI/WinMain.cpp(3494)`: `gC_DD.Init(hwnd, ...)` — `CSDLGraphics`에
  대응하는 `Init()` 메서드가 없음 (창 생성 등 실질 초기화 로직 필요)
- `VS_UI/WinMain.cpp(3523)`: `gC_DDSurface.InitBacksurface()` —
  `CSpriteSurface`에 대응 메서드 없음
- `VS_UI/WinMain.cpp(1451)`: `gC_DDSurface.ShowFPS(...)` —
  `CSpriteSurface`에 대응 메서드 없음
- `Client/ClientFunction.cpp(452, 457)`: `GammaBox565`/`GammaBox555` —
  `CSpriteSurface`에 감마 보정 렌더링 기능 자체가 아직 없음
  (`Gamma4Pixel565`/`Gamma4Pixel555` 유틸리티 함수만 존재, 이를 사용하는
  `GammaBox*` 메서드는 미구현)
